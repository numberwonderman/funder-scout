@extends('layouts.app', ['title' => 'Settings · Kindred'])
@section('content')
<section class="page-intro"><div><span class="eyebrow">Settings</span><h1>Research connections</h1><p class="lede">See which data paths are available to the research workflow. Secrets are never displayed here.</p></div></section>
<section class="section settings-list"><div><span><strong>Strands agent service</strong><small>AI orchestration and evidence synthesis</small></span><span class="connection {{ $demoMode ? '' : 'connected' }}">{{ $demoMode ? 'Demo mode' : 'Live mode' }}</span></div><div><span><strong>Amazon Bedrock</strong><small>Model access uses the server's AWS credentials</small></span><span class="connection {{ $demoMode ? '' : 'connected' }}">{{ $demoMode ? 'Not used in demo' : 'Configured by service' }}</span></div><div><span><strong>CRM contacts</strong><small>HubSpot records are checked before public people research</small></span><span class="connection {{ $crmConfigured ? 'connected' : '' }}">{{ $crmConfigured ? 'Connected' : 'Not connected' }}</span></div><div><span><strong>Evidence policy</strong><small>Unsupported prospects fail closed and are never substituted</small></span><span class="connection connected">Enforced</span></div></section>
<section class="section notice"><strong>Connection management</strong><br><span class="small">Credentials are managed in the server environment. This page intentionally does not expose secret values.</span></section>
@endsection
