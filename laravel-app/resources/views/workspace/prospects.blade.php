@extends('layouts.app', ['title' => 'Prospects · Kindred'])
@section('content')
<section class="page-intro"><div><span class="eyebrow">Prospects</span><h1>Funding opportunities</h1><p class="lede">Every saved prospect passed evidence and relevance validation. Use feedback to keep the list useful.</p></div><a class="button" href="{{ route('research.index') }}">New research</a></section>
<section class="quality-strip"><div><strong>{{ $quality['north_star'] }}%</strong><span>Evidence-supported path to money</span></div><div><strong>{{ $quality['primary_verified'] }}%</strong><span>Primary-source verified</span></div><div><strong>{{ $quality['unknown_edges'] }}</strong><span>Unknown decision links</span></div><div><strong>{{ $quality['contradictions'] }}</strong><span>Contradicted claims</span></div></section>
<section class="section"><div class="panel">@forelse($prospects as $prospect)<a class="prospect-card" href="{{ route('prospects.show', $prospect) }}"><div class="score-number">{{ $prospect->fit_score }}<small>Fit score</small></div><div><span class="match">{{ str_replace('_',' ', $prospect->opportunity_status) }}</span><div class="prospect-name">{{ $prospect->name }}</div><span class="muted small">{{ $prospect->researchRun->campaign->title }} · {{ $prospect->researchRun->campaign->organization->name }}</span></div><div class="ask"><strong>{{ $prospect->recommended_ask_min ? '$'.number_format($prospect->recommended_ask_min).'–$'.number_format($prospect->recommended_ask_max) : 'Investigate' }}</strong><span>{{ $prospect->feedback->last()?->status ? str_replace('_',' ', $prospect->feedback->last()->status) : 'No feedback' }}</span></div><span class="arrow">→</span></a>@empty<div class="empty panel-pad">No validated prospects yet.</div>@endforelse</div></section>
@endsection
